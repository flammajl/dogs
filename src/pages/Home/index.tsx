import React from 'react';
import Feed from '../../components/Feed';

const Home: React.FC = () => (
  <section className="container mainContainer">
    <Feed />
  </section>
);

export default Home;
